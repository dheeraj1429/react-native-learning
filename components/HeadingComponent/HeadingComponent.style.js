import { StyleSheet } from "react-native";
import { COLORS, SIZES } from "../../constants";

export const styles = StyleSheet.create({
   heading: {
      fontFamily: "Poppins-SemiBold",
      fontSize: SIZES.sizeExtraLarge,
      color: COLORS.mainCl,
   },
});
